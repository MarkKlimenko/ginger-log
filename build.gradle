buildscript {
    ext {
        coverageExclusion = [
                '**/**',
        ]

        // library properties
        libraryGroup = 'com.markklim.libs'
        libraryArchivesBaseName = "ginger-log"
        libraryVersion = "2.1.0-TEST"

        // versions
        kotlinVersion = "1.9.25"
        kotlinReactorVersion = "1.2.2"

        springBootVersion = "3.3.1"
        springManagementVersion = "1.1.5"
        springCloudVersion = "2023.0.2"

        feignReactorVersion = "4.2.1"
        feignVersion = "3.1.0"

        caffeineVersion = "3.1.6"

        junitVersion = "5.9.0"
        awaitilityVersion = "4.2.0"
        codeQualityVersion = "1.2.1"

        jettyHttpClientVersion = "4.0.5"
    }
}
